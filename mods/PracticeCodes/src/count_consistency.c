#include <common.h>

typedef struct Streak
{
    int totalAmount;
    int successAmount;
}Streak;
Streak streak;

extern bool counter_mode;

void CountStreakUpdate(void)
{
    if (counter_mode == true)
    {
        
    }
}